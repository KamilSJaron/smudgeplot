language: python3
python:
  - "3.6"
  - "3.7.2"
cache: pip
install:
  - pip install -r requirements.txt && pip install PySAIS==1.0.4 && python3 setup.py install
branches:
  only:
    - dev
script:
  - make test
